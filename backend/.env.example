# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
# For EC2 deployment, set this to your frontend URL or use * for all origins
CORS_ORIGIN=*

# API Configuration
API_PREFIX=/api
